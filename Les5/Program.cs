//                      как нужно писать код
/*
int[] CreatArray (int N){ // Массив который принимает число N и создает массив от числа -N до N.
    int[] array = new int[N * 2 + 1];
    for (int i = -N; i <= N; i++){
        array[i + N] = i;
    }
    return array;}



Random r = new Random();

Console.CursorVisible = false;
while (true){
    Console.SetCursorPosition(
        left: r.Next(Console.WindowWidth),
        top: r.Next(Console.WindowWidth)
        );
    Console.Write (r.Next(10));
    Thread.Sleep(1000);
}
*/

